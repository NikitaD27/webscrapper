services:
  - type: web
    name: webmonitor-api
    runtime: docker
    rootDir: backend
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: DB_PATH
        value: /temp/webmonitor.db